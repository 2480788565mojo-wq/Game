# 项目 Agent 行为准则

本文件适用于在当前项目根目录及其子目录中工作的所有 AI Agent。项目级指令优先于 Agent 的默认行为；若与用户明确要求冲突，以用户要求为准，并记录冲突及其影响。

## 一、基本行为准则

- 先理解需求、现状和约束，再采取行动。
- 以真实文件、命令输出、测试结果和可复现步骤为依据，不凭猜测下结论。
- 保持改动最小、边界清晰，避免与当前任务无关的重构。
- 所有工作说明、结果、问题分析和最终报告默认使用中文；技术名词可保留英文。
- 涉及不确定内容时明确标注假设、风险和待确认事项。
- 不因代码“看起来合理”、命令成功或测试通过就跳过需求和异常路径检查。

## 二、开发规范

- 修改前先读取相关代码、配置、文档、测试和调用链，遵循现有项目结构与命名约定。
- 新增功能应保持模块职责单一，避免巨型类、巨型函数、循环依赖、重复实现和无意义抽象。
- 对外接口、数据结构、状态转换和错误处理必须保持一致；变更接口时同步检查所有调用方。
- 输入必须校验，空值、非法值、边界值、重复请求、异常和超时必须有明确行为。
- 不硬编码 API Key、密码、Token 或其他敏感信息；使用项目既有的安全配置方式。
- Unity 代码避免在每帧执行昂贵操作、网络请求或 LLM 调用，注意对象生命周期、资源释放和状态一致性。
- AI Agent、Tool Calling 和游戏状态修改必须进行权限校验、参数校验、循环上限和失败恢复设计。
- 注释说明原因、约束和非显而易见的决策，避免重复代码本身含义的注释。

## 三、标准工作流程

1. **检查上下文**：查看项目结构、`AGENTS.md`、`.agents`、`.codex`、需求文档、技术方案、相关代码和测试。
2. **明确计划**：说明目标、拟改文件、风险、验证方式和不在范围内的内容；需求或架构存在重大歧义时先询问用户。
3. **小步实施**：按模块逐步修改，保持每次改动可审查；不要覆盖或回滚用户已有的未提交改动。
4. **验证结果**：运行与改动匹配的 Unit、Integration、E2E、构建、静态检查或手工验证，并记录实际命令和结果。
5. **检查差异**：确认没有意外文件、敏感信息、格式问题或业务逻辑外改动。
6. **交付说明**：说明改了什么、为什么改、如何验证、测试限制、已知风险和相关文件位置。

## 四、测试与审查 Agent 约束

- 测试 Agent 只测试、分析和报告，不修改业务代码、配置或资源。
- 代码审查 Agent 只读审查并提出建议；问题必须有代码位置和证据，不为凑数量制造问题。
- 对抗式审查 Agent 只能在授权的本地测试环境进行攻击，不攻击外部系统、真实用户或生产服务。
- 测试或审查未覆盖的场景必须明确列出，不能将“未执行”判定为“通过”。
- 生成的报告和临时产物应放在项目约定位置，避免污染源码和资源目录。

## 五、禁止事项

- 未经用户明确授权，不删除、覆盖、重置、迁移或批量改写现有文件。
- 不修改与当前任务无关的业务代码，不借机重构或升级依赖。
- 不伪造测试结果、日志、代码位置、API 能力或需求依据。
- 不绕过权限、校验、审计、沙箱或安全策略来“让功能跑通”。
- 不在源码、提交信息、报告或日志中泄露密钥、个人数据或内部凭据。
- 不使用生产数据进行破坏性测试，不发送未经授权的外部消息或请求。
- 发现可能造成数据丢失、安全问题或不可逆影响的操作时，必须先停止并说明风险。

## 六、完成标准

只有在目标已实现、相关验证已完成、改动范围清楚且报告准确时，才能声明任务完成。若受环境、依赖、权限或缺少需求资料影响无法完成，必须明确说明阻塞原因、已完成部分和下一步所需条件。

## 七、上下文管理

1. 不要让关键项目知识只存在于当前对话上下文中。
2. 重要的需求、架构决策、技术决策、任务进度和已知问题必须及时写入项目文件。
3. 当当前上下文接近容量上限时，优先进行上下文压缩，并确保关键状态已经持久化到项目文件。
4. 当任务阶段发生重大变化，或者当前对话已经包含大量无关历史信息时，优先建议开启新的对话。
5. 新对话开始时，首先读取 MISSION.md、AGENTS.md 以及与当前任务相关的 PRD、Spec、Plan 和状态文件。
6. 不要假设新对话能够访问之前对话中的所有信息。
7. 不得因为上下文不足而自行猜测之前已经确定的需求、架构或技术决策。
