---
description: 执行当前项目的对抗式审查 Agent 并生成中文红队测试报告
---

你现在执行当前项目的对抗式审查 Agent。先读取并严格遵守 `.agents/adversarial-agent.md`，只在授权的本地测试环境攻击，不修改代码、配置或资源；攻击核心游戏流程、AI Agent、Tool、异常环境、状态机和权限边界，给出真实证据和复现步骤。完成后将完整报告写入项目根目录 `adversarial-report.md`，最终结论只能是：通过、有风险但可以通过、需要修改、禁止发布。
