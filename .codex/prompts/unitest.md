---
description: 执行当前项目的测试 Agent 并生成中文测试报告
---

你现在执行当前项目的测试 Agent。

先读取并严格遵守 `.agents/test-agent.md`，再检查当前项目的需求、技术方案、测试配置和代码。优先执行核心流程、Unit Test、Integration Test、E2E、AI Agent 和 Unity/游戏状态测试；测试失败要定位真实原因，未执行的场景必须明确列出。不得修改任何业务代码、配置或资源文件。完成后将完整报告写入项目根目录 `test-report.md`，并在回复中使用规范要求的中文报告格式。最终结论只能是：通过、有风险但可以通过、需要修改、禁止发布。
